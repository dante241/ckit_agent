<!-- ACTIVE feature pointer.
     Dòng đầu tiên KHÔNG-comment, KHÔNG-blank = slug feature đang active.
     Trống (không có dòng slug) = không có feature nào active.
     `/feature switch <slug>` và `8sync feature switch <slug>` ghi lại dòng này.
     Feature dữ liệu sống ở agents/planning/<slug>/. -->
SLUG
